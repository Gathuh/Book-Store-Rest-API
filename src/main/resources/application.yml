spring:
  application:
    name: project




  datasource:
    url: jdbc:mysql://localhost:3306/book_store_db # RMEMBER TO CREATE THE DATABASE
    username: root
    password: Macharia@2030
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # or create-drop if you want to recreate tables on restart
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true


security:
  user:
    name: Gathu
    password: 4522
server:
  port: 9090